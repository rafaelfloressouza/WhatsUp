<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/light_gray</item>
        <item name="colorPrimaryDark">@color/light_gray</item>
        <item name="colorAccent">@color/green</item>
        <item name="android:actionOverflowButtonStyle">@style/Widget.ActionButton.Overflow</item>
        <item name="android:listDivider">@drawable/custom_list_divider</item>
    </style>

    <!--For the Dashboard's Toolbar-->
    <style name="CustomToolbarTheme" parent="ThemeOverlay.AppCompat.Dark.ActionBar">
        <item name="android:background">@color/dark_grey</item>
        <item name="colorControlNormal">@color/light_gray</item>
    </style>

    <!--For the Chats's Toolbar-->
    <style name="ChatsToolbarTheme" parent="ThemeOverlay.AppCompat.Dark.ActionBar">
        <item name="android:background">@color/dark_grey</item>
    </style>

    <!--For the Chats's Toolbar-->
    <style name="FloatingButtonTheme" parent="ThemeOverlay.AppCompat.Dark.ActionBar">
        <item name="android:backgroundTint">@color/base_green</item>
    </style>


    <style name="CustomEditTextTheme" parent="ThemeOverlay.AppCompat.Dark.ActionBar">
        <item name="android:textColor">@color/white</item>
        <item name="colorControlNormal">@color/light_gray</item>
        <item name="colorControlActivated">@color/white</item>
        <item name="errorTextColor">@color/green</item>
        <item name="errorIconTint">@android:color/transparent</item>
    </style>


<!--    Changing overflow button's icon.-->
    <style name="Widget.ActionButton.Overflow" parent="@android:style/Widget.Holo.ActionButton.Overflow">
        <item name="android:src">@drawable/ic_settings_base_green</item>
    </style>

</resources>
